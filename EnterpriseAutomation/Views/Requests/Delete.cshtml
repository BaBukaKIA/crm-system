@model EnterpriseAutomation.Models.ServiceRequest
@{ ViewData["Title"] = "Удаление заявки"; }
<h1>Удаление заявки</h1>
<div class="alert alert-danger">Удалить заявку клиента «@Model.Client?.Name»?</div>
<form asp-action="Delete" method="post"><input type="hidden" asp-for="Id" /><button class="btn btn-danger">Удалить</button> <a class="btn btn-secondary" asp-action="Index">Отмена</a></form>
