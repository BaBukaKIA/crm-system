@model EnterpriseAutomation.Models.ServiceRequest
@{
    ViewData["Title"] = "Удаление заявки";
}

<div class="page-head">
    <h1>Удаление заявки</h1>
    <p>Это действие удалит выбранное обращение.</p>
</div>

<div class="delete-panel">
    <div class="alert alert-danger">Удалить заявку #@Model.Id?</div>
    <form asp-action="Delete" method="post" class="form-actions">
        <input type="hidden" asp-for="Id" />
        <button class="btn btn-danger">Удалить</button>
        <a class="btn btn-secondary" asp-action="Index">Отмена</a>
    </form>
</div>
